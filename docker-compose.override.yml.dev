## Override configuration for development environment
version: "2"

services:
  php:
    build:
      args:
        - BUILD_ENV=dev
    depends_on:
      - mongo
      - workspace

  nginx:
    ports:
      - "80:80"

